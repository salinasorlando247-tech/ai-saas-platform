export async function postToTikTok(videoUrl, caption, tokens) {
  // TikTok Upload API placeholder
  // chunk upload + publish
  return {
    platform: "tiktok",
    status: "posted",
    url: "https://tiktok.com/..."
  };
}
