version: "3.8"

services:
  backend:
    build:
      context: ./Backend
    container_name: ai-saas-backend
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env
    restart: unless-stopped
