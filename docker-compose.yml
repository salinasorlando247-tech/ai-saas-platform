version: "3.9"
services:
  app:
    build: ./backend
    container_name: ai_app
    restart: always
    env_file: .env
    ports:
      - "5000:5000"
    depends_on:
      - redis
  redis:
    image: "redis:7-alpine"
    container_name: ai_redis
    ports:
      - "6379:6379"
    restart: always
